# Module kmp-ble-codec

Format-agnostic codec layer for typed serialization and deserialization of BLE characteristic data.

Provides composable `Encoder` and `Decoder` abstractions that map raw BLE byte arrays to strongly-typed Kotlin data classes, independent of any specific serialization format.
